# Project Scope Example

This folder demonstrates a project scope.

```bash
cd examples/project-scope
eval "$(aiscope hook zsh)"
aiscope status
```

The env file path is declared in `.aiscope.toml` and points to the local vault.
